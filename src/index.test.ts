import { test, describe } from 'node:test'
import assert from 'assert'
import lib from './index.ts'

describe('lib', () => {
  test('should exist', () => {
    console.log('akjhakjh')
    assert.ok(lib)
  })
})
